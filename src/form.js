import React, { Component } from 'react';

 export default class Form extends Component {
    render(props) {
        return (
            <div>
               helllo {this.props.text}
            </div>
        )
    }
}
